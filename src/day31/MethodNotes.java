package day31;

   public class MethodNotes {
       public static void main(String[] args) {
           /**
            * We learned :
            * 1. print method:
            * a. System.out.print(print something);
            * b. System.out.println(print something);
            *2.Scanner methods:
            * a.Scanner scan = new Scanner(System.in);
            * String name = scan.next();//scan - is an address(leash to command the object) to an object
            * int age = scan.nextInt();
            *
            * 3.String methods:
            * String str = "Hello" ;
            * char firstChar = str.charAt(0)
            * str.toLowercase()
            * and so on...........
            *
            * Arrays class method :
            *
            * Arrays.toString(  your array object here) ;
            *
            * Arrays.sort(your array object here)
            *
            * Arrays.deepToString( your multi d array object here) ;
            *
            * // observation :
            * method that accept external data when being called
            * method that does not accept external data when being called
            * -------
            * method that generate some type of result that can be saved
            * method that does not generate any result, just plain action
            *
            */
       }
}
